# Server
PORT=4000
NODE_ENV=development
CORS_ORIGIN=http://localhost:5173

# Database (MySQL, used by Prisma)
DATABASE_URL="mysql://user:password@localhost:3306/assetsouq"

# Auth
JWT_SECRET=change-me
JWT_EXPIRES_IN=8h

# Mail (Nodemailer)
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
MAIL_FROM="AssetSouq <no-reply@example.com>"
